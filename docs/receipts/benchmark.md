# Reviewer-Gate Benchmark Receipt

Generated by `scripts/benchmark_reviewer_gate.py --write`, running 20 real, executed scenarios against this repo's actual `MultiAgentOrchestrator` / `ReviewerAgent` / `SharedContext` code (`src/multi_agent_system_pattern/`). No numbers below are hand-written; regenerate this file with:

```bash
python scripts/benchmark_reviewer_gate.py --write
```

## Methodology

This measures the reviewer gate's **mechanical** behavior — whether `ReviewerAgent.run()`'s real, deterministic presence-check (`context.by_role("research")`, `"analysis"`, `"writer"` all present and non-empty) approves or rejects a given agent roster and task. It is **not** a measurement of LLM output quality: every `Agent` implementation in this repo is a deterministic stub with no external API calls, matching this repo's sibling curriculum-stub repos and the existing README/demo "simulated metrics" framing. This benchmark is additive to that existing framing, not a replacement for it.

Four conditions, 20 total scenarios:

- **orchestrator_complete** (8 scenarios) — real `MultiAgentOrchestrator` with the full `[ResearchAgent, AnalystAgent, WriterAgent, ReviewerAgent]` roster (2 of the 8 built in reordered construction order, as a config-variation check) across 8 varied task requests.
- **single_generalist** (8 scenarios) — the same 8 tasks, but the roster is `[GeneralistAgent, ReviewerAgent]`: a real, deterministic `GeneralistAgent` (new code in `src/multi_agent_system_pattern/benchmark.py`) that answers directly in one pass, without producing separate research/analysis/writer artifacts. The same, unmodified `ReviewerAgent` evaluates it.
- **orchestrator_incomplete** (2 scenarios) — real orchestrator run with `AnalystAgent` deliberately left out of the roster, to confirm the gate catches an incomplete specialist roster too.
- **duplicate_role_guard** (2 scenarios) — real construction attempts with a duplicated agent role, confirming `MultiAgentOrchestrator.__init__`'s `ValueError("Agent roles must be unique")` guard fires.

## Per-scenario results

| Scenario | Condition | Roster (construction order) | Request | Result |
|---|---|---|---|---|
| `orch-complete-01` | orchestrator_complete | research, analysis, writer, reviewer | Prepare a customer support automation plan | approved=True — `approved: final output is grounded and complete` |
| `orch-complete-02` | orchestrator_complete | research, analysis, writer, reviewer | Create an enterprise AI automation brief | approved=True — `approved: final output is grounded and complete` |
| `orch-complete-03` | orchestrator_complete | analysis, research, writer, reviewer | Draft a Q3 infrastructure cost-reduction memo | approved=True — `approved: final output is grounded and complete` |
| `orch-complete-04` | orchestrator_complete | research, analysis, writer, reviewer | Summarize competitive positioning for a new product launch | approved=True — `approved: final output is grounded and complete` |
| `orch-complete-05` | orchestrator_complete | research, analysis, writer, reviewer | Produce an incident postmortem for a payments outage | approved=True — `approved: final output is grounded and complete` |
| `orch-complete-06` | orchestrator_complete | analysis, research, writer, reviewer | Write a migration plan from monolith to microservices | approved=True — `approved: final output is grounded and complete` |
| `orch-complete-07` | orchestrator_complete | research, analysis, writer, reviewer | Assess vendor risk for a new SaaS procurement | approved=True — `approved: final output is grounded and complete` |
| `orch-complete-08` | orchestrator_complete | research, analysis, writer, reviewer | Draft an onboarding guide for new platform engineers | approved=True — `approved: final output is grounded and complete` |
| `generalist-01` | single_generalist | generalist, reviewer | Prepare a customer support automation plan | approved=False — `rejected: missing specialist artifacts` |
| `generalist-02` | single_generalist | generalist, reviewer | Create an enterprise AI automation brief | approved=False — `rejected: missing specialist artifacts` |
| `generalist-03` | single_generalist | generalist, reviewer | Draft a Q3 infrastructure cost-reduction memo | approved=False — `rejected: missing specialist artifacts` |
| `generalist-04` | single_generalist | generalist, reviewer | Summarize competitive positioning for a new product launch | approved=False — `rejected: missing specialist artifacts` |
| `generalist-05` | single_generalist | generalist, reviewer | Produce an incident postmortem for a payments outage | approved=False — `rejected: missing specialist artifacts` |
| `generalist-06` | single_generalist | generalist, reviewer | Write a migration plan from monolith to microservices | approved=False — `rejected: missing specialist artifacts` |
| `generalist-07` | single_generalist | generalist, reviewer | Assess vendor risk for a new SaaS procurement | approved=False — `rejected: missing specialist artifacts` |
| `generalist-08` | single_generalist | generalist, reviewer | Draft an onboarding guide for new platform engineers | approved=False — `rejected: missing specialist artifacts` |
| `orch-incomplete-01` | orchestrator_incomplete | research, writer, reviewer | Prepare a customer support automation plan | approved=False — `rejected: missing specialist artifacts` |
| `orch-incomplete-02` | orchestrator_incomplete | research, writer, reviewer | Produce an incident postmortem for a payments outage | approved=False — `rejected: missing specialist artifacts` |
| `duplicate-guard-01` | duplicate_role_guard | research, research, writer, reviewer | Prepare a customer support automation plan | guard_raised=True — `Agent roles must be unique` |
| `duplicate-guard-02` | duplicate_role_guard | writer, analysis, writer, reviewer | Create an enterprise AI automation brief | guard_raised=True — `Agent roles must be unique` |

## Aggregate reviewer-gate outcome rate per condition

| Condition | Scenarios | Positive outcomes | Rate |
|---|---|---|---|
| Orchestrator (complete specialist roster) | 8 | 8 (approved) | 100% |
| Single generalist (baseline) | 8 | 0 (approved) | 0% |
| Orchestrator (incomplete roster — AnalystAgent missing) | 2 | 0 (approved) | 0% |
| Duplicate-role construction guard | 2 | 2 (guard fired) | 100% |

The orchestrator's complete specialist roster clears the real reviewer gate on every scenario tried here; the single-generalist baseline — evaluated by the exact same, unmodified reviewer gate — does not, because it never populates the per-role artifacts the gate checks for. The incomplete-roster and duplicate-role-guard scenarios confirm the gate and the constructor guard both correctly fire when they should, rather than passing everything by default.
